{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <div class="container">

        <h1>Sciences</h1>

        <!-- Liste des sciences -->
        {% for science in sciences %}

        <div class="science-thumb">
            <h2><a href="{{ path("science_detail", { 'id' : science.id}) }}">{{ science.title }}</a></h2>
            <p>
                <!-- description -->
                {{ science.description }}
            </p>
            <p>
                <!-- Lien vers la page détail science -->
                <a href="#" class="btn btn-sm btn-default">
                    Voir les publications
                </a>
            </p>
        </div>
        {% endfor %}
    </div>
{% endblock %}